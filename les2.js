let arr = [1,2,3];

let user = employeeArr[4]

for (let key in user){
    console.log(typeof key)
}

for (let key in user){
    console.log(user[key])
}

for (let key in user){
    console.log(`key: $(key), value: $(value)`)
}

Object.keys(user)

for(let key in arr){ console.log(key) }

arr['some'] = 'some'

for (let value of arr){
    console.log(valuee)
}

Object.values(user);

console.log(Object.entries(user));


//
function getId(){
    let user = [];

for (let value of employeeArr){

    user.push(value.id);
}
    return user;
};

//Object.values(user);

let ArrFunct = function(){
    let user = employeeArr;
    employeeArr.map(item => employee = new Employee(student.id, student.name, student.surname, student.salary, student.workExperience, student.isPrivileges, student.gender);)
     for (let in )

    return
    //array.forEach(element => {
        
    });
}



let arr = employeeArr.map((user) => {
    let arr = [];
    for (let key in user){
        arr.push(key);
    }
    return arr;
})
console.log(arr);

//employeeArr.map((user) => Object.keys(user))

let arr = employeeArr.map((user) function(){
    Object.keys(user));
})

let arr = employeeArr.map((user) => Object.value(user))